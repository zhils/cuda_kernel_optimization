# Softmax

## 数学定义

对长度为 C 的每一行向量 x：

```
softmax(x_i) = exp(x_i - max(x)) / Σ_j exp(x_j - max(x))
```

数值稳定版本通过减去 `max(x)` 来避免 `exp()` 溢出。

## 理论性能分析

### 算术强度

| 指标 | 公式 | 数值 |
|------|------|------|
| 数据搬运量 | 读输入 + 写输出 ≈ 2 × R × C × 4 bytes | 8RC bytes |
| 计算量 | R × (C 次比较 + C 次 exp + C 次加法 + C 次除法) | ~4RC FLOPs |
| 算术强度 | 4RC / 8RC | **0.5 FLOP/Byte** |

### Roofline 分类

算术强度约为 0.5 FLOP/Byte，Softmax 在各种输入规模下都属于典型 **内存受限**。
优化重点是提升 DRAM 带宽利用率并减少全局内存访问轮次与访存形式。

### 理论峰值

- RTX 5060 Ti DRAM 带宽：~448 GB/s（量级）
- 理想下界（单次读 x + 单次写 y）：约 2 × R × C × 4 bytes 搬运量

## 优化版本

### V0：朴素实现（每行单线程）

**文件：** `softmax_v0_naive.cu`

每个线程顺序处理整行：3 次遍历（max → exp+sum → normalize）。

- **问题：** 行内无并行，对大 C 场景浪费 GPU 资源
- **问题：** 每行对全局内存多次遍历，且对 `y` 存在中间写回与再读

### V1：减少全局访存 + 优化访存形式

**文件：** `softmax_v1.cu`

每行一个 block，线程协同做 max / sum 归约；相对 V0 仅做全局内存两类优化：

1. **减少全局读写：** 当单行长度 `cols` 可在共享内存中暂存整行 `exp` 时，避免「先写 `y=exp` 再读 `y` 归一化」的中间全局往返，对 `y` 从多次全局接触降为一次写入。
2. **优化访存形式：** 对只读 `x`（及 stream 路径下对 `y` 的读）使用 `__ldg`；在 `cols % 4 == 0` 且地址 16 字节对齐时使用 `float4` 合并访问。

`cols` 过大无法暂存时回退为三阶段 stream 路径，仍使用 `__ldg` 与 `float4`。

### V2：Online Softmax（单次累积）

**文件：** `softmax_v2.cu`

在线归一化（Milakov & Gimelshein, 2018）：在单遍扫描中维护运行中的 `max` 与 `sum`，发现更大 `max` 时对历史部分和重缩放。

- **核心思路：** 相对多遍遍历，减少全局内存遍历次数
- **数值稳定：** 通过在线重缩放保证等价于「先减 max 再 exp」的稳定 softmax

### V3：Warp Shuffle + 向量化

**文件：** `softmax_v3.cu`

面向 **单 warp 可覆盖行宽** 的场景（如 `cols ≤ 128`）：

- `__shfl_sync` / `__shfl_down_sync` 做 warp 内 max/sum 归约，减少共享内存
- `float4` 向量化加载/写回，降低访存指令与事务数

### cuDNN 参考

**文件：** `softmax_cudnn_ref.cu`

`cudnnSoftmaxForward`（`CUDNN_SOFTMAX_ACCURATE` 模式）作为库基线。

## 横向对比

**文件：** `softmax_benchmark_all.cu`

聚合多版本与 cuDNN 的计时对比，结果写入 `data/results/softmax_all_comparison.csv`（需本地运行生成）。

## 性能结果

### 执行时间对比 (ms)

| 行数 (R) | 列数 (C) | V0 Naive | V1 SharedMem | V2 Online | V3 Warp+Vec | cuDNN |
|----------|----------|----------|---------------|-----------|-------------|-------|
| 64 | 512 | 0.125 | 0.016 | 0.013 | 0.007 | 12.86 |
| 64 | 1024 | 0.235 | 0.015 | 0.010 | 0.007 | 0.09 |
| 64 | 4096 | 0.963 | 0.017 | 0.010 | 0.010 | 0.23 |
| 128 | 512 | 0.165 | 0.017 | 0.008 | 0.011 | 0.10 |
| 128 | 1024 | 0.326 | 0.015 | 0.010 | 0.010 | 0.13 |
| 128 | 4096 | 1.255 | 0.021 | 0.007 | 0.007 | 0.42 |
| 256 | 512 | 0.292 | 0.024 | 0.014 | 0.008 | 0.13 |
| 256 | 1024 | 0.570 | 0.024 | 0.009 | 0.008 | 0.22 |
| 256 | 4096 | 2.266 | 0.034 | 0.013 | 0.011 | 0.83 |
| 512 | 768 | 0.536 | 0.036 | 0.011 | 0.011 | 0.33 |
| 512 | 1024 | 0.609 | 0.036 | 0.014 | 0.011 | 0.42 |
| 512 | 4096 | 2.466 | 0.046 | 0.020 | 0.012 | 1.65 |

### 吞吐量对比 (GFLOPS)

| 行数 (R) | 列数 (C) | V0 Naive | V1 SharedMem | V2 Online | V3 Warp+Vec | cuDNN |
|----------|----------|----------|---------------|-----------|-------------|-------|
| 64 | 512 | 0.52 | 4.08 | 4.97 | 9.54 | ~0 |
| 64 | 1024 | 0.56 | 8.76 | 13.00 | 18.69 | 1.43 |
| 64 | 4096 | 0.54 | 30.95 | 51.73 | 50.80 | 2.27 |
| 128 | 512 | 0.79 | 7.93 | 15.95 | 11.75 | 1.30 |
| 128 | 1024 | 0.80 | 17.36 | 25.67 | 26.06 | 2.02 |
| 128 | 4096 | 0.84 | 48.95 | 150.23 | 142.47 | 2.51 |
| 256 | 512 | 0.90 | 10.75 | 19.05 | 31.28 | 2.09 |
| 256 | 1024 | 0.92 | 21.60 | 61.65 | 63.08 | 2.34 |
| 256 | 4096 | 0.93 | 62.00 | 157.44 | 191.35 | 2.53 |
| 512 | 768 | 1.47 | 22.05 | 69.40 | 70.39 | 2.39 |
| 512 | 1024 | 1.72 | 29.52 | 75.85 | 94.77 | 2.49 |
| 512 | 4096 | 1.70 | 90.40 | 213.25 | 363.46 | 2.54 |

### 关键结论

1. **V2 Online Softmax** 在大矩阵场景下性能最佳，512×4096 达到 **470 GFLOPS**
2. **cuDNN** 在小矩阵场景有固定开销，不适合小 batch 大 hidden dim
3. **V3 Warp+Vec** 在中等规模矩阵表现优秀，特别是 256×4096 达到 **246 GFLOPS**
4. **V0 Naive** 受限于行内串行计算，SM 利用率低

## 产物路径

- **源文件：** `softmax_v0_naive.cu`、`softmax_v1.cu`、`softmax_v2.cu`、`softmax_v3.cu`、`softmax_cudnn_ref.cu`、`benchmark_all.cu`
- **可执行文件：** `build/bin/` 下目标名以 `softmax/CMakeLists.txt` 为准（可能与上述 `.cu` 文件名略有差异，需与 CMake 中 `add_executable` 一致才能成功编译）
- 性能 CSV：`data/results/`

## NVIDIA 参考 API

- **cuDNN：** `cudnnSoftmaxForward` / `cudnnSoftmaxBackward`
